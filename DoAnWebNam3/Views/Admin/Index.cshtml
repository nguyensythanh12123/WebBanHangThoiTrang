@model IEnumerable<DoAnWebNam3.Models.SanPham>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Share/LayOutChung.cshtml";
}

<div class="container">
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr style="font-size:14px">
        <th>
            Mã Sản Phẩm
        </th>
        <th>
            Tên Sản Phẩm
        </th>
        <th>
           Ảnh Sản Phẩm
        </th>
        <th>
           Đơn Vị Tính
        </th>
        <th>
           Gía Bán
        </th>
        <th>
           Mã Nhà Cung Cấp
        </th>
        <th>
            Mã Loại
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MaSP)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenSP)
        </td>
        <td>
            <img class="rounded" src="~/Image/AnhThoiTrangTreEm/@Html.DisplayFor(modelItem => item.Anh)" width="170px" height="200px" />
            
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GiaBan)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaNCC)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaLoai)
        </td>
        <td class="btn-group" style="width:150px">
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }, new {@class="btn btn-danger" }) <br />
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ },new {@class="btn btn-danger" }) <br />*@
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ },new {@class="btn btn-danger" })
        </td>
    </tr>
}

</table>
</div>
